workspace:
  base: /artifacts
  path: /repo
pipeline:
  build:
    image: vinchauhan/my-iib:rc
    commands:
      - cd CustomerDatabaseV1
      - export PATH=$PATH:/build/
      - /usr/local/bin/iib_env.sh
      - goiib clean
      - goiib compile
    environment:
      - LICENSE=accept  
      